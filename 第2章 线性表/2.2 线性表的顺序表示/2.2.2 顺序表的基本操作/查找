//按位查找
int GetElem(SeqList L, int i){
    return L.data[i-1];
}

//按值查找
int LocateElem(SeqList L, int i){
    for(int i=0;i<=L.length-1;i++){
        if(L.data[i]==e)
            return i+1;
    return 0;
}
